services:
  html5-studio:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - html5-studio-data:/data
    environment:
      - NODE_ENV=production
      - DATA_DIR=/data
      - PORT=3001

volumes:
  html5-studio-data:
